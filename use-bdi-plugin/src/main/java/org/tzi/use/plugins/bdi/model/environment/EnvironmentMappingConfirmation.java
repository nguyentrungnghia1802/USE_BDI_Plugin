package org.tzi.use.plugins.bdi.model.environment;

/** User decision state for a persisted environment mapping candidate. */
public enum EnvironmentMappingConfirmation {
    CANDIDATE,
    CONFIRMED
}
