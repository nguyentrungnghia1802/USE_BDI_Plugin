package org.tzi.use.plugins.bdi.model.mapping;

/** Mapping relation types persisted in a .bdimap.json document. */
public enum MappingKind {
    AGENT_CLASS,
    AGENT_OBJECT,
    ACTION_OPERATION,
    PARAMETER,
    RECEIVER_OBJECT,
    BELIEF_ATTRIBUTE
}
