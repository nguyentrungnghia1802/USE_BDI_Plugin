package org.tzi.use.plugins.bdi.model.organization;

/** A candidate is review evidence, while only a confirmed mapping is a binding. */
public enum OrganizationMappingConfirmation {
    CANDIDATE,
    CONFIRMED
}
